#include <functional>
#include <vector>
#include <random>

using T = std::vector<std::vector<int>>;
using L = double;

// Recombination Operators ----------------------------------------------------------